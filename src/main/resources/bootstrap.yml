---
spring:
    application:
        name: Gateway-Service
    profiles:
      active: local
    cloud:
      config:
        uri: ${CONFIG_SERVER_URL:https://user:7c97f478-7471-48a2-8304-93b14527a1c4@sarbesh-config-server.herokuapp.com}
        enabled: true

server:
    port: ${PORT:8080}
